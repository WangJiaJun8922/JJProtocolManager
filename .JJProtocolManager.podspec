pod::Spec.new do |s|
s.name        = "JJProtocolManager"
s.summary     = "JJJ."
	    s.requires_arc = true
	        s.version     = "0.0.1"
		    s.platform    = :ios, "6.0"
		        s.license     = "MIT"
			    s.source      = {
				        :git => "https://github.com/WangJiaJun8922/JJProtocolManager.git", 
					    :tag => "#{s.version}" 
						        }
end
